import React from "react";

import styles from "./styles.module.scss";

interface MenuButtonProps {}

export const MenuButton = () => {
  return <button className={styles.menu_button}>index</button>;
};
